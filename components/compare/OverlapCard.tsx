import { Stock } from "@/types/stock";
export function OverlapCard({ stocks }: { stocks: Stock[] }) { const etfs=stocks.filter(s=>s.assetType==="ETF"); const overlap=etfs.length>=2 ? Math.min(82, Math.round(etfs.reduce((sum,s)=>sum+(s.topTenHoldingsWeight ?? 0),0)/etfs.length)) : 0; return <section className="card p-5"><h3 className="font-black">ETF Holdings Overlap</h3><div className="mt-3 text-4xl font-black">{overlap}%</div><p className="mt-2 text-sm text-slate-600">Estimated mock overlap across selected ETF top holdings.</p>{overlap>50 && <div className="mt-4 rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm font-semibold text-amber-800">These ETFs may not provide enough diversification.</div>}</section>; }

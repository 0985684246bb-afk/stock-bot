"use client";
import { mockStocks } from "@/data/mockStocks";
export function CompareSelector({ selected, onChange }: { selected: string[]; onChange: (tickers: string[]) => void }) { return <div className="card p-4"><p className="mb-3 text-sm font-semibold text-slate-500">Choose 2 to 4 tickers</p><div className="flex flex-wrap gap-2">{["SCHG","VOO","QQQM","SPY","00878.TW","006208.TW","0050.TW","AAPL","MSFT","NVDA"].map(t => <button key={t} onClick={()=> selected.includes(t) ? onChange(selected.filter(x=>x!==t)) : selected.length<4 && onChange([...selected,t])} className={`rounded-xl px-3 py-2 text-sm font-bold ${selected.includes(t)?"bg-blue-600 text-white":"bg-slate-100"}`}>{t}</button>)}</div><p className="mt-3 text-xs text-slate-500">Mock universe: {mockStocks.length} securities.</p></div>; }

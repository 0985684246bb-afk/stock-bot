import { InvestmentNote } from "@/types/note";
export function NoteList({ notes }: { notes:InvestmentNote[] }) { return <div className="grid gap-4">{notes.map(n=><article key={n.id} className="card p-5"><div className="flex flex-wrap justify-between gap-2"><h3 className="text-xl font-black">{n.ticker} · {n.action}</h3><p className="text-sm text-slate-500">{n.date} · confidence {n.confidenceLevel}</p></div><p className="mt-3 text-slate-700">{n.reason}</p><div className="mt-3 grid gap-2 text-sm text-slate-500 md:grid-cols-3"><p><strong>Holding:</strong> {n.expectedHoldingPeriod}</p><p><strong>Risks:</strong> {n.riskFactors}</p><p><strong>Sell trigger:</strong> {n.sellTrigger}</p></div><p className="mt-2 text-sm text-slate-500"><strong>Thesis:</strong> {n.relatedThesis}</p></article>)}</div>; }

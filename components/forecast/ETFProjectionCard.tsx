import { calculateETFProjection } from "@/lib/forecast";
import { formatCurrency } from "@/lib/formatters";
import { Stock } from "@/types/stock";
export function ETFProjectionCard({ stock, years }: { stock:Stock; years:number }) { if(stock.assetType!=="ETF") return null; const r=calculateETFProjection({currentInvestment:10000,monthlyContribution:300,expectedAnnualReturn:0.07,expenseRatio:stock.expenseRatio??0,dividendYield:stock.dividendYield??0,dividendReinvestmentPercent:1,years}); return <section className="card p-5"><h3 className="font-black">ETF Long-Term Projection</h3><p className="text-sm text-slate-500">ETF forecasts use compounding assumptions, expense drag, dividends, and contributions—not single PE fair value.</p><div className="mt-4 grid gap-3 md:grid-cols-3"><Box k="Conservative 4%" v={r.bear}/><Box k="Base 7%" v={r.base}/><Box k="Optimistic 10%" v={r.bull}/></div><div className="mt-4 grid gap-3 md:grid-cols-3"><Box k="Future Value" v={r.futureValue}/><Box k="Total Gain" v={r.totalGain}/><Box k="Expense Drag" v={r.expenseDragEstimate}/></div></section>; }
function Box({k,v}:{k:string;v:number}){return <div className="rounded-xl bg-slate-50 p-3"><p className="text-xs font-bold uppercase text-slate-500">{k}</p><p className="text-xl font-black">{formatCurrency(v)}</p></div>}

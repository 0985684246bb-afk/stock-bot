import { ForecastScenario } from "@/types/forecast";
import { formatCurrency, formatPercent } from "@/lib/formatters";
export function ScenarioForecast({ scenarios }: { scenarios: ForecastScenario[] }) { return <div className="grid gap-4 lg:grid-cols-3">{scenarios.map(s=><section key={s.type} className="card p-5"><p className="text-sm font-bold uppercase text-slate-500">{s.type} case</p><div className="mt-2 text-2xl font-black">{formatCurrency(s.priceLow)} - {formatCurrency(s.priceHigh)}</div><p className="text-sm text-slate-500">Expected return: {formatPercent(s.expectedReturn)}</p><div className="mt-4"><p className="font-bold">Assumptions</p><ul className="text-sm text-slate-600">{s.assumptions.map(a=><li key={a}>• {a}</li>)}</ul></div><div className="mt-3"><p className="font-bold">Risks</p><ul className="text-sm text-slate-600">{s.risks.map(r=><li key={r}>• {r}</li>)}</ul></div></section>)}</div>; }

import { Stock } from "@/types/stock";
import { formatLargeNumber, formatPercent } from "@/lib/formatters";
export function ETFHoldingsCard({ stock }: { stock: Stock }) { if (stock.assetType !== "ETF") return null; return <section className="card p-5"><h3 className="font-black">ETF Metrics & Holdings</h3><div className="mt-4 grid gap-3 md:grid-cols-4"><Metric k="Expense Ratio" v={formatPercent(stock.expenseRatio)}/><Metric k="AUM" v={formatLargeNumber(stock.aum)}/><Metric k="Holdings" v={`${stock.numberOfHoldings ?? "—"}`}/><Metric k="Top 10 Weight" v={`${stock.topTenHoldingsWeight ?? "—"}%`}/></div><p className="mt-4 text-sm text-slate-500">Tracking: {stock.trackingIndex} · Inception: {stock.inceptionDate}</p><div className="mt-4 grid gap-2 md:grid-cols-2">{stock.topHoldings?.map(h=><div key={h.ticker} className="flex justify-between rounded-xl bg-slate-50 p-3 text-sm"><span>{h.ticker} {h.name}</span><strong>{h.weight}%</strong></div>)}</div></section>; }
function Metric({k,v}:{k:string;v:string}){return <div className="rounded-xl bg-slate-50 p-3"><p className="text-xs font-bold uppercase text-slate-500">{k}</p><p className="font-black">{v}</p></div>}

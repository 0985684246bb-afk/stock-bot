import { formatCurrency, formatLargeNumber, formatNumber, formatPercent } from "@/lib/formatters";
import { Stock } from "@/types/stock";
export function StockMetricCards({ stock }: { stock: Stock }) { const items = [["Market Cap", formatLargeNumber(stock.marketCap)], ["PE Ratio", formatNumber(stock.peRatio)], ["Forward PE", formatNumber(stock.forwardPE)], ["EPS", formatNumber(stock.eps)], ["Revenue Growth", formatPercent(stock.revenueGrowth)], ["Profit Margin", formatPercent(stock.profitMargin)], ["Dividend Yield", formatPercent(stock.dividendYield)], ["Payout Ratio", formatPercent(stock.payoutRatio)], ["Beta", formatNumber(stock.beta)], ["52W High", formatCurrency(stock.fiftyTwoWeekHigh)], ["52W Low", formatCurrency(stock.fiftyTwoWeekLow)]]; return <section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map(([k,v])=><div key={k} className="card p-4"><p className="text-xs font-bold uppercase text-slate-500">{k}</p><div className="mt-2 text-xl font-black">{v}</div></div>)}</section>; }
